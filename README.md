# About the Package 

The package will be returning a object with two function add() and multiply. Both of these receive two arguments and then provides the Desired Result .

# Usage 

Install Command : npm install --save npm-bcebti

var bcebti=require("npm-bcebti");
var x=bcebti.add(5,10);
console.log(x);

The Output will be : 15